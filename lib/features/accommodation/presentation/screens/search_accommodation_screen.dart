import 'package:flutter/material.dart';

class SearchAccommodationScreen extends StatelessWidget {
  const SearchAccommodationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('SearchAccommodationScreen is working'),
      ),
    );
  }
}